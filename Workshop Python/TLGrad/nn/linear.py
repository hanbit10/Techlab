import numpy.random as npr

def init_parameters(layers, random_state=npr.RandomState(0), scale=0.1):
    # TODO: layers is a description of the MLP e.g. [768, 250, 10 ]
    # TODO: return a list of random initialized [A, b] 
    # TODO: e.g. [ [(768, 250), (1)], [(250, 10), (1)] ] denoting dimensions here
    # TODO: use random_state.randn(input_dim, output_dim) to generate a random matrix of dims
    # TODO: multiply scale with every random generated matrix/vector
    parameters = []

    return parameters

